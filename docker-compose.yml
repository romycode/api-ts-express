version: '3.8'

services:
    express-api:
        container_name: api_node-express
        image: api_node-express
        build:
            context: .
            dockerfile: docker/node-lts/dev/Dockerfile
